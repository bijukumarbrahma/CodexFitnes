# TODO - Fix login

- [x] Update Netlify function path rewrite to ensure Express `req.path` becomes `/auth/login` etc.
- [ ] Add minimal server-side logging for auth/login routing (path/method).
- [x] Deploy/update code for path rewrite + add auth/login diagnostics.
- [x] Update TODO status for logging + routing changes.

- [ ] Deploy and test login; confirm network response and token storage.


