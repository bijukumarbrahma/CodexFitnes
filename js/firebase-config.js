/*
  Firebase config is served dynamically by the server from .env variables.
  This file is a placeholder — the actual config is injected at runtime.
  For Netlify deployment, set build command: node scripts/build-config.js
*/
